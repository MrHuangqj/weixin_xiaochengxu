<!-- <button bindtap='getLocation'>授权位置</button>
<button bindtap='cityWeather(广州)'>城市天气</button>
<view>{{position.longitude}}</view>
<view>{{position.latitude}}</view>
<view>{{city}}</view>
<map id="map" longitude="{{position.longitude}}" latitude="{{position.latitude}}" scale="14" controls="{{controls}}" bindcontroltap="controltap" markers="{{markers}}" bindmarkertap="markertap" polyline="{{polyline}}" bindregionchange="regionchange" show-location
  style="width: 100%; height: 300px;"></map> -->
<view style='padding:0 20rpx;'>
  <view class='weather' style='font-size:50rpx;'>{{weather.city}}</view>
  <view class='weather' style='font-size:100rpx;'>{{weather.wendu}}</view>
  <view>{{weather.ganmao}}</view>
  <view style='padding:0 20rpx;'>
    <view wx:for="{{weather.forecast}}" wx:key="index" style='font-size:30rpx;margin:10rpx 0;display:flex;justify-content:space-between;'>
      <view>{{item.date}}</view>
      <view>{{item.type}}</view>
      <view>{{item.high}}~{{item.low}}</view>
    </view>
    <!-- <view>风力:{{item.fengli}}</view> -->
    <!-- <view>风向:{{item.fengxiang}}</view> -->
  </view>
</view>